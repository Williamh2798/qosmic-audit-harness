#!/usr/bin/env npx tsx
/**
 * Re-render report.md from a previous audit-package.json (legacy).
 * Prefer: npm run audit -- --manifest audits/aud_xxx
 */
console.log(`Reports are now written directly as markdown.
Run: npm run audit -- --manifest audits/aud_xxx/manifest.json`);
